# Обязательное правило авто-сохранения (Git)
После КАЖДОГО успешного изменения файлов или написания кода, ты ОБЯЗАН автоматически выполнить в терминале команду:
git add . && git commit -m "AI Auto-update"
Это критически важно, чтобы в проекте не висели несохраненные изменения (буквы M и U).